import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact — CMO",
  description:
    "Get in touch with the CMO team. Reach us on X (@surojitpvt) for support, feedback, and partnership inquiries.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
